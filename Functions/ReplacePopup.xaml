<Window x:Class="Text_Editor.ReplacePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Text_Editor"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="ReplacePopup" Height="100" Width="300"> 
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Margin="5" Padding="0" Content="Select:"/>
        <Label Grid.Column="0" Grid.Row="1" Margin="5" Padding="0" Content="Replace with:"/>

        <TextBox Grid.Column="1" Grid.Row="0" Margin="5" Name="select"  />
        <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Name="withWhat"/>

        <Button Grid.Column="2" Grid.Row="0" Content="Replace" Margin="5" Click="Replace_Click"/>
        <Button Grid.Column="2" Grid.Row="1" Content="Cancel"  Margin="5" Click="Cancel_Click" />
    </Grid>
</Window>
